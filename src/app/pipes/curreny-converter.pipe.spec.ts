import { CurrenyConverterPipe } from './curreny-converter.pipe';

describe('CurrenyConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrenyConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
